# Database Configuration
DATABASE_URL=postgresql://user:password@localhost:5432/securevault
PGHOST=localhost
PGPORT=5432
PGUSER=user
PGPASSWORD=password
PGDATABASE=securevault

# Session Secrets
SESSION_SECRET=your_super_secret_session_key_here

# App URL Environment Variable (for emails, links, or internal reference)
SECRET_URL=https://example.com/secret

# Server Config
PORT=5000
